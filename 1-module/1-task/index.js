/**
 * Factorial
 * @param {number} n
 * @returns {number}
 */
function factorial(n) {
  function factorial(n) {
    let i = 1;
    while (n > 1){
      i = i * n;
      n --;
    }
    return i
  }

  factorial(3)
}
